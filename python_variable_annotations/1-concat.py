#!/usr/bin/env python3
"""
    Simple concat function using two strings
"""


def concat(str1: str, str2: str) -> str:
    """
        Concatenate two strings
        str1(str) & str2(str) and return the result
    """
    return str1 + str2
